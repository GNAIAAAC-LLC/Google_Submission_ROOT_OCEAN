// GNAIAAAC LLC VOICE AUTOMATION ENGINE
// CONNECTED TO SSGPT6 CORE

const recognition = new webkitSpeechRecognition();
recognition.onresult = (event) => {
    const command = event.results[0][0].transcript.toLowerCase();
    console.log("[SSGPT6]: Voice Command Received: " + command);
    
    if (command.includes("show energy")) {
        window.location.hash = "#unit-012";
        alert("ACCESSING ZERO-CARBON BLUEPRINTS...");
    }
    if (command.includes("authorize family")) {
        alert("AUTHENTICATION SUCCESSFUL: SHANTA LEGACY UNLOCKED.");
    }
};
// START VOICE AUTOMATION
recognition.start();
